# NEURAL MODEL IDENTIFICATION

In this folder there is all needed material to train a model to approximate a dynamic model,
starting from the generation of training trajectories to the real training phase.